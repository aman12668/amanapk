import 'package:flutter/material.dart';

class IOM extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Coming Soon....'),
          centerTitle: true,
          backgroundColor: Colors.red,
        ),
      );
}